name: Leaflow 自动登录签到

on:
  workflow_dispatch:
  schedule:
    - cron: "5 0,12 * * *"   # 北京时间 08:05

concurrency:
  group: leaflow-checkin
  cancel-in-progress: true

jobs:
  checkin:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      TZ: Asia/Shanghai
      SOCKS5_INFO: ${{ secrets.SOCKS5_INFO }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git build-essential libnss3 libatk1.0-0 libatk-bridge2.0-0 \
          libcups2 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libgbm1 libasound2 wget || true

      # ===============================
      # 3proxy：源码编译
      # ===============================
      - name: 🧩 Build & Install 3proxy
        run: |
          git clone https://github.com/3proxy/3proxy.git
          cd 3proxy
          make -f Makefile.Linux
          sudo cp bin/3proxy /usr/local/bin/3proxy
          3proxy --version || true

      # ===============================
      # 启动本地 SOCKS5 转发
      # ===============================
      - name: 🚀 Start local SOCKS5 proxies
        run: |
          mkdir -p /tmp/3proxy
          CONF=/tmp/3proxy/3proxy.cfg
          echo "daemon" > $CONF
          echo "nserver 8.8.8.8" >> $CONF
          echo "nscache 65536" >> $CONF
          echo "timeouts 1 5 30 60 180 1800 15 60" >> $CONF
          echo "log /tmp/3proxy/3proxy.log" >> $CONF
          echo "auth strong" >> $CONF

          IFS=',' read -ra PROXIES <<< "$SOCKS5_INFO"
          PORT=1081
          USERS_LINE="users"

          for p in "${PROXIES[@]}"; do
            USERPASS=${p%@*}
            HOSTPORT=${p#*@}
            USER=${USERPASS%%:*}
            PASS=${USERPASS#*:}
            HOST=${HOSTPORT%%:*}
            RPORT=${HOSTPORT#*:}

            USERS_LINE="$USERS_LINE $USER:CL:$PASS"
            echo "socks -p$PORT -i127.0.0.1 -e$HOST" >> $CONF
            echo "✅ 映射 $HOST:$RPORT → 127.0.0.1:$PORT"
            PORT=$((PORT+1))
          done

          echo $USERS_LINE >> $CONF

          sudo 3proxy $CONF &
          sleep 3

      - name: 🔍 Verify local proxy ports
        run: |
          ss -lntp | grep 108 || true

      - name: 📦 Install Python dependencies
        run: |
          pip install -U pip
          pip install playwright requests[socks] pynacl cryptography
          playwright install chromium

      - name: ▶️ Run Leaflow Checkin
        env:
          LEAFLOW_ACCOUNTS: ${{ secrets.LEAFLOW_ACCOUNTS }}
          LEAFLOW_COOKIES:  ${{ secrets.LEAFLOW_COOKIES }}
          REPO_TOKEN:       ${{ secrets.REPO_TOKEN }}
          TG_BOT_TOKEN:     ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID:       ${{ secrets.TG_CHAT_ID }}
          CONFIG_PASSWORD: ${{ secrets.CONFIG_PASSWORD }}
        run: |
          python -u leaflow/Leaflow_checkin.py

      - name: 📸 Upload Leaflow screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: leaflow-login-fail
          path: |
            **/*.png
