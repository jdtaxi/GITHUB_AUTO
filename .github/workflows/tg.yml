name: Process Text from TG

on:
  repository_dispatch:
    types: [process-text]

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Process message
        id: process
        run: |
          set +e
          INPUT="${{ github.event.client_payload.text }}"
          CHAT_ID="${{ github.event.client_payload.chat_id }}"

          echo "收到原始内容: $INPUT"

          # 按行转换数组
          mapfile -t lines <<< "$INPUT"

          OUTPUT="处理结果:\n"
          for i in "${!lines[@]}"; do
            OUTPUT+="$((i+1)): ${lines[i]}\n"
          done

          # 输出 workflow 输出变量
          echo "output<<EOF" >> $GITHUB_OUTPUT
          echo -e "$OUTPUT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Send result back to TG (safe)
        if: always()
        run: |
          CALLBACK_URL="https://tg-github-bot.greenwave1987.workers.dev/callback"
          CHAT_ID="${{ github.event.client_payload.chat_id }}"
          OUT="${{ steps.process.outputs.output || '处理失败' }}"

          # 使用 jq 安全生成 JSON，支持中文/英文/特殊字符/多行
          JSON=$(echo "$OUT" | jq -R --slurp --arg chat_id "$CHAT_ID" '{chat_id:$chat_id, text:.}')

          # 重试 3 次，避免临时网络失败
          for i in {1..3}; do
            curl -X POST "$CALLBACK_URL" -H "Content-Type: application/json" -d "$JSON" && break
            sleep 2
          done
